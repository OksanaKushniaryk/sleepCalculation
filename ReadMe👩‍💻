🧩 Що таке Node.js

Node.js — це середовище виконання JavaScript поза браузером. Воно дозволяє запускати .js-файли прямо на комп’ютері (наприклад, тести, калькулятори, сервери тощо).

🪜 Крок 1. Перевір, чи Node.js вже встановлений

a. Відкрий Cursor.

b. У нижній частині або через меню відкрий термінал (shortcut: <kbd>Ctrl</kbd> + <kbd></kbd> або <kbd>Cmd</kbd> + <kbd></kbd> на macOS).

c. У терміналі введи: node -v

d. Якщо ти бачиш, наприклад, v20.12.2, — Node.js вже встановлений ✅
Якщо ж отримаєш помилку command not found, — переходимо до наступного кроку.

🪄 Крок 2. Завантаж і встанови Node.js

a. Відкрий офіційний сайт:
🔗 https://nodejs.org

b. Ти побачиш дві кнопки:

c. LTS (Long Term Support) — стабільна версія, рекомендована для більшості користувачів

Current — найновіша, але експериментальна
👉 Обери LTS.

Завантаж файл для своєї операційної системи:

Windows — .msi

macOS — .pkg

d. Запусти інсталятор і просто натискай Next, залишаючи налаштування за замовчуванням.
(Головне — не знімай галочку "Add to PATH", це потрібно, щоб команда node працювала в терміналі.)

💻 Крок 3. Перезапусти Cursor

Після завершення установки закрий і відкрий Cursor знову, щоб він “побачив” нову змінну середовища PATH.

🧠 Крок 4. Перевір установку

У терміналі введи:

node -v
npm -v

Якщо обидві команди повертають версії (наприклад v20.12.2 та 10.5.0), то все ✅

🧾 npm (Node Package Manager) встановлюється автоматично разом із Node.js — це менеджер пакетів для встановлення бібліотек.


📂 Крок 5. Створи перший проєкт у Cursor

У Cursor створи нову папку, наприклад my-node-app.

Відкрий у ній термінал і введи:

npm init -y

Це створить файл package.json — “паспорт” твого проєкту.

Створи файл index.js у цій же папці.

Додай туди код:

console.log("Hello from Node.js!");

Запусти його з терміналу:

node index.js

✅ У консолі має з’явитись:

Hello from Node.js!



